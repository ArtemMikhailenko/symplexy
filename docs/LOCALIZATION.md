# 🌍 Локалізація (i18n)

## Підтримувані мови

- 🇺🇦 Українська (`uk`) - за замовчуванням
- 🇬🇧 Англійська (`en`)

## Структура

```
messages/
  ├── uk.json  # Українські переклади
  └── en.json  # Англійські переклади
```

## Використання в компонентах

### Client Components

```tsx
"use client";

import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('section');
  
  return (
    <div>
      <h1>{t('title')}</h1>
      <p>{t('description')}</p>
    </div>
  );
}
```

### Server Components

```tsx
import { useTranslations } from 'next-intl';

export default async function MyServerComponent() {
  const t = await useTranslations('section');
  
  return (
    <div>
      <h1>{t('title')}</h1>
    </div>
  );
}
```

## Навігація з локалізацією

Використовуйте локалізовані компоненти навігації:

```tsx
import { Link, useRouter, usePathname } from '@/lib/navigation';

// Link компонент
<Link href="/about">About</Link>

// useRouter
const router = useRouter();
router.push('/contact');

// Зміна мови
router.replace(pathname, { locale: 'en' });
```

## Переключення мов

Компонент `LanguageSwitch` автоматично:
- Визначає поточну мову
- Зберігає поточний шлях
- Перемикає між uk/en

```tsx
<LanguageSwitch />
```

## URL структура

- **Українська (за замовчуванням):** `/` → `/about` → `/blog`
- **Англійська:** `/en` → `/en/about` → `/en/blog`

## Додавання нових перекладів

1. Відкрийте `messages/uk.json` та `messages/en.json`
2. Додайте новий ключ у обидва файли:

```json
{
  "newSection": {
    "title": "Назва розділу",
    "description": "Опис розділу"
  }
}
```

3. Використовуйте в компоненті:

```tsx
const t = useTranslations('newSection');
<h1>{t('title')}</h1>
```

## Metadata локалізація

Metadata автоматично локалізується в `app/[locale]/layout.tsx`:

```tsx
export async function generateMetadata({ params }) {
  const { locale } = await params;
  const messages = await getMessages({ locale });
  
  return {
    title: messages.metadata.title,
    description: messages.metadata.description,
  };
}
```

## Доступні переклади

### Header
- `header.nav.home` - Головна / Home
- `header.nav.aiTools` - ШІ інструменти / AI Tools
- `header.nav.howItWorks` - Як це працює / How It Works
- `header.nav.blog` - Блог / Blog
- `header.nav.about` - Про нас / About Us
- `header.cta` - Підібрати інструменти / Find Tools

### Hero
- `hero.title` - Заголовок
- `hero.description` - Опис
- `hero.cta.primary` - Основна кнопка
- `hero.cta.secondary` - Вторинна кнопка

### Numbers, How It Works, FAQ, Blog, Contact, Footer
Див. повний список у файлах `messages/*.json`

## Тестування

1. Відкрийте сайт: `http://localhost:3000`
2. Клікніть на перемикач мов (Ua/En) у хедері
3. Перевірте, що:
   - URL змінюється (`/` → `/en`)
   - Контент перекладається
   - Навігація працює
   - Metadata оновлюється

## Проблеми та вирішення

### Переклади не відображаються
- Перевірте, чи існує ключ у обох файлах перекладів
- Переконайтесь, що шлях до ключа правильний
- Перезавантажте dev сервер

### URL не змінюється при перемиканні мови
- Використовуйте `Link` з `@/lib/navigation` замість `next/link`
- Для router використовуйте `useRouter` з `@/lib/navigation`

### Секція залишається на одній мові
- Переконайтесь, що компонент обгорнутий у `NextIntlClientProvider`
- Перевірте, що `useTranslations` викликається у client component

## Best Practices

1. ✅ **Завжди додавайте переклади в обидва файли**
2. ✅ **Використовуйте змістовні ключі** (`hero.title`, не `text1`)
3. ✅ **Групуйте за секціями** (`header.nav`, `footer.links`)
4. ✅ **Використовуйте локалізовану навігацію** (`@/lib/navigation`)
5. ✅ **Тестуйте на обох мовах** перед deploy
6. ❌ **Не хардкодьте текст** в компонентах
7. ❌ **Не забувайте про aria-labels** для доступності

## Розширення

### Додавання нової мови (напр. польська)

1. Додайте локаль у `i18n.ts`:
```ts
export const locales = ['uk', 'en', 'pl'] as const;
```

2. Створіть `messages/pl.json` з перекладами

3. Оновіть `LanguageSwitch.tsx` для підтримки третьої мови

4. Перебудуйте проект: `npm run build`
